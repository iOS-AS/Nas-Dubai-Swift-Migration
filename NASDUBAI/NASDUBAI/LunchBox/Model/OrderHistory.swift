//
//  OrderHistory.swift
//  NAS
//
//  Created by Naveeth's on 13/07/21.
//

import Foundation
//import ObjectMapper
/*
class CanteenOrdersHistory: Mappable {

    var id : Int?
    var delivery_date : String?
    var total_amount : String?
    var status : Int?
    var canteen_preordered_items : [CanteenOrderedItems]?

    required init?(map: Map) {

    }

    func mapping(map: Map) {

        id <- map["id"]
        delivery_date <- map["delivery_date"]
        total_amount <- map["total_amount"]
        status <- map["status"]
        canteen_preordered_items <- map["canteen_preordered_items"]
    }
}

class CanteenOrderedItems: Mappable {

    var id : Int?
    var item_id : Int?
    var quantity : Int?
    var portal : Int?
    var item_status : Int?
    var item_total : Double?
    var item_name : String?
    var price : String?
    var available_quantity : Int?
    var item_description : String?
    var cancellation_time_exceed : Int?

    required init?(map: Map) {

    }

    func mapping(map: Map) {

        id <- map["id"]
        item_id <- map["item_id"]
        quantity <- map["quantity"]
        portal <- map["portal"]
        item_status <- map["item_status"] //0 cancel, 1 active, 2 means Delivered
        item_total <- map["item_total"]
        item_name <- map["item_name"]
        price <- map["price"]
        available_quantity <- map["available_quantity"]
        item_description <- map["item_description"]
        cancellation_time_exceed <- map["cancellation_time_exceed"]
    }
}
*/

